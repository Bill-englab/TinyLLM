# GPTQ 4-bit 离线量化日志

- 时间:2026-06-18 23:03:54
- 源模型:`/mnt/d/LLM_Project/code/model/llama-3.2-1b-instruct/LLM-Research/Llama-3___2-1B-Instruct`
- 输出模型:`/mnt/d/LLM_Project/code/model/llama-3.2-1b-gptq-4bit`
- 参数:bits=4, group_size=128, desc_act=False
- 校准样本数:130

## 关键指标

- 量化耗时:**4.64 分钟**
- FP16 模型体积:4.61 GB
- GPTQ 模型体积:**0.98 GB**
- 压缩比:**4.72×**

## 输出对比

### Prompt 1: 用三句话解释什么是量化(quantization)。

**FP16 baseline:**
```
量化是指将量转化为数字或数值的过程，通常涉及将量的量值转换为数字或整数。
```

**GPTQ 4-bit:**
```
量化是指将量数变为数字或数值，通常涉及到数字化、计数、计数等过程。
```

### Prompt 2: Write a Python function to compute fibonacci numbers.

**FP16 baseline:**
```
**Fibonacci Sequence Computation Function**
=====================================

Here's a Python function to compute the nth Fibonacci number using an iterative approach:

```python
def fibonacci(n):
    """
    Compute the nth Fibonacci number.

    Args:
        n (int): The position of the Fibonacci number to compute.

    Returns:
        int: The nth Fibonacci number.

    Raises:
        ValueError: If n is a negative integer.
    """
    if n < 0:
        raise ValueError("n must
```

**GPTQ 4-bit:**
```
**Fibonacci Sequence Computation**
=====================================

The Fibonacci sequence is a series of numbers where a number is the sum of the two preceding ones, usually starting with 0 and 1. Here's a Python function to compute the nth Fibonacci number:

```python
def fibonacci(n):
    """
    Compute the nth Fibonacci number.

    Args:
    n (int): The position of the Fibonacci number to compute.

    Returns:
    int: The nth Fibonacci number.

    Raises:
   
```

### Prompt 3: 解释 GPTQ 量化的核心思想。

**FP16 baseline:**
```
GPT (Generative Pre-Trained Transformer) 是一种强大的自然语言处理模型，基于Transformer architecture。它被广泛应用于各种自然语言处理任务，包括文本生成、文本转换、文本相似性评估等。GPT 量化的核心思想包括：

1. **Transformer Architecture**: GPT 使用Transformer architecture，一个典型的自注意力机制，能够处理序列数据（文本）并生成相似的输出
```

**GPTQ 4-bit:**
```
GPT 量化是一种量化投资策略，旨在通过使用计算机化的计算机程序来进行投资决策。核心思想是将投资决策过程转化为计算机化的过程，使得投资者可以根据计算机程序化的数据和模型来进行投资决策。

GPT 量化的核心思想包括：

1. **数据驱动**: GPT 量化的核心是数据驱动。它将投资决
```
